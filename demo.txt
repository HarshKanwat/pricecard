This is a GitHub learining
that highlighted dot on name of file shows it's not saved yet
and when you saved it it gone.
(git init) command give you a empty repositery in git GitHub
 (git remote -v)
 after entering it into bas it shows
 origin  https://github.com/HarshKanwat/Optional.git (fetch) (for download)
 origin  https://github.com/HarshKanwat/Optional.git (push) (for uplode)
 
Gitadd is used to send file to local directory to staying area and then local pepo (which is also called saving)
after doing it we use push command to push our file to remote github
git checkout -b "testing"
        it changed the destination to main to testing

and after testing we will push our commang by(

    git add .
    git commit -m "now testing to main branch"
    git push origin tell you to push data between to branches

)
git checkout -b "branch name"
to create a new branch